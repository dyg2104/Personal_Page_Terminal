== README

A personal website with the following functionality:

Rails
- Displays personal bio, resume, and projects

Backbone:
- Adds a navigation bar to the home page
- Displays personal bio, resume, projects, and directory upon input
- Accomplishes this by creating a new view and appending it to the current view